---
pagination:
  data: stories
  size: 250
  alias: story
permalink: /avvenimenti/index.html
---
{% extends 'layouts/base.njk' %}

{% set generic = generics['event'] %}

{% block head %}
	<title>{{ generic.title }} · Aphroconfuso</title>
	<meta content="{{ metadata.description }}" name="description">
	<meta property="og:image" content="{{ metadata.settings.imagesUrl }}{{ metadata.settings.socialImage }}">
{% endblock %}

{% block content %}
	<article class="generic contents event">
		<header>
			<ul><li class="header-label">&nbsp;</li></ul>
			<h1>{{ generic.title }}</h1>
			<h2>&nbsp;</h2>
		</header>
		<div class="container">
			<section id="grid-body">
				<div class="generic-body-text">
					{{ generic.body | prettifyMaltese | safe }}
					{% set promoFrom = "Avveniment" %}
					{% set promos = stories | selectattr("listableEvent") %}
					{% set useSquare = true %}
					{% include "_promo_list.njk" %}
				</div>
			</section>
		</div>
	</article>
	<script>
		reportingTitle = "{{ event.title }}";
	</script>
{% endblock %}
