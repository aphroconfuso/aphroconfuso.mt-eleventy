<ul class="promo-list">
	{% set promoFrom = promoFrom or page.url %}
	{% set promoTo = promo.sequenceEpisodeTitle or promo.title %}
	{% for promo in promos %}
		<li>
			<a class="promo {{ promo.monthYear | slugify }} promo-{{ promo.type | slugify }} story-{{ promo.id }}" href="/{{ promo.slug }}/" onclick="_paq.push(['trackEvent', 'Promo', 'minn: {{ promoFrom }}', 'għal: {{ promoTo }}', {{ loop.index }}]);">
				{% include "_promo_header.njk" %}
				<div class="{{ promo.cssClass }}">
					{% if promo.type === 'Poezija' %}
						<p>{{ promo.description | prettifyMaltese | versifyDescription | safe }}</p>
					{% else %}
						<p>{{ promo.description | prettifyMaltese | safe }}</p>
					{% endif %}
				</div>
			</a>
		</li>
	{% endfor %}
</ul>
